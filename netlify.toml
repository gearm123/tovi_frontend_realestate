[build]
  command = "npm run build"
  publish = "dist"

# Redirect rules (including SPA fallback) live in public/_redirects
# so legacy WordPress 301s are evaluated before the /* → index.html rewrite.
